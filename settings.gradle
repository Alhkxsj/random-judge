import org.gradle.api.initialization.resolve.RepositoriesMode
import org.gradle.kotlin.dsl.repositories

plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.4.0"
}

repositories {
    google()
    mavenCentral()
}

rootProject.name = "随机判断"
include(":app")